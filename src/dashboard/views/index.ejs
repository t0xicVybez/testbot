<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Bot Dashboard</title>
    <link href="/css/output.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900">
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg text-center max-w-2xl w-full mx-4">
            <% if (!user) { %>
                <div class="mb-8">
                    <h1 class="text-4xl font-bold text-white mb-4">Welcome to Discord Bot Dashboard</h1>
                    <p class="text-gray-400 text-lg mb-8">Take control of your Discord bot with our powerful dashboard. Configure settings, view statistics, and manage your server with ease.</p>
                    <a href="/auth/discord" 
                       class="bg-indigo-600 hover:bg-indigo-700 text-white px-8 py-4 rounded-lg transition-colors inline-flex items-center text-lg group">
                        <i class="fab fa-discord mr-3 text-2xl group-hover:scale-110 transition-transform"></i>
                        Login with Discord
                    </a>
                </div>

                <!-- Features Section -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-12 text-left">
                    <div class="bg-gray-700 p-6 rounded-lg">
                        <i class="fas fa-shield-alt text-3xl text-indigo-400 mb-4"></i>
                        <h3 class="text-xl font-semibold text-white mb-2">Server Management</h3>
                        <p class="text-gray-400">Powerful tools to manage your Discord server with ease.</p>
                    </div>
                    <div class="bg-gray-700 p-6 rounded-lg">
                        <i class="fas fa-chart-bar text-3xl text-green-400 mb-4"></i>
                        <h3 class="text-xl font-semibold text-white mb-2">Analytics</h3>
                        <p class="text-gray-400">Detailed insights and statistics about your server activity.</p>
                    </div>
                    <div class="bg-gray-700 p-6 rounded-lg">
                        <i class="fas fa-cog text-3xl text-yellow-400 mb-4"></i>
                        <h3 class="text-xl font-semibold text-white mb-2">Custom Commands</h3>
                        <p class="text-gray-400">Create and manage custom commands for your server.</p>
                    </div>
                    <div class="bg-gray-700 p-6 rounded-lg">
                        <i class="fas fa-bell text-3xl text-red-400 mb-4"></i>
                        <h3 class="text-xl font-semibold text-white mb-2">Notifications</h3>
                        <p class="text-gray-400">Stay updated with real-time server notifications.</p>
                    </div>
                </div>
            <% } else { %>
                <div class="mb-8">
                    <div class="relative w-24 h-24 mx-auto mb-4">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" 
                             alt="Profile" 
                             class="rounded-full w-full h-full object-cover ring-4 ring-indigo-500">
                        <div class="absolute bottom-0 right-0 bg-green-500 w-6 h-6 rounded-full border-4 border-gray-800"></div>
                    </div>
                    <h1 class="text-3xl font-bold text-white mb-2">Welcome back, <%= user.username %>!</h1>
                    <p class="text-gray-400 mb-8">Ready to manage your Discord bot?</p>
                    <div class="grid gap-4">
                        <a href="/dashboard" 
                           class="bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-3 rounded-lg transition-colors flex items-center justify-center group">
                            <i class="fas fa-chart-line mr-2 group-hover:translate-x-1 transition-transform"></i>
                            Go to Dashboard
                        </a>
                        <a href="/dashboard/commands" 
                           class="bg-gray-700 hover:bg-gray-600 text-white px-6 py-3 rounded-lg transition-colors flex items-center justify-center group">
                            <i class="fas fa-terminal mr-2 group-hover:translate-x-1 transition-transform"></i>
                            Configure Commands
                        </a>
                        <a href="/logout" 
                           class="bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg transition-colors flex items-center justify-center group">
                            <i class="fas fa-sign-out-alt mr-2 group-hover:translate-x-1 transition-transform"></i>
                            Logout
                        </a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html> 